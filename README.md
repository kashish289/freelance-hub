# 🚀 Freelance Marketplace (Fiverr Clone)

A full-stack marketplace for freelancers and clients built with the MERN stack.

## ✨ Features
- **Dual Roles:** Buyer and Seller dashboards.
- **Secure Payments:** Stripe API integration.
- **Real-time Chat:** Socket.io messaging.
- **Gig Management:** Full CRUD for services.

## 🛠️ Tech Stack
- **Frontend:** React, Tailwind CSS, React Query
- **Backend:** Node.js, Express, MongoDB
- **Auth:** JSON Web Tokens (JWT)

## 🚦 Getting Started
1. Clone the repo: `git clone https://github.com/user/repo.git`
2. Install dependencies: `npm install` in both `/client` and `/server`.
3. Create a `.env` file in `/server` with your `MONGO_URI` and `STRIPE_KEY`.
4. Run `npm start`.
